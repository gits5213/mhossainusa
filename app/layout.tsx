import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import Navbar from "@/components/Navbar";
import Footer from "@/components/Footer";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "MD MOSARRAF HOSSAIN - IT Automation & Monitoring Engineer/Project Manager",
  description:
    "13+ years of experience in Software Quality Assurance and Software Testing. Expert in manual and automation testing, IT automation, monitoring, and project management.",
  keywords: [
    "Software Test Analyst",
    "QA Engineer",
    "Software Testing",
    "Quality Assurance",
    "HP ALM",
    "Selenium",
    "Test Automation",
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        <Navbar />
        {children}
        <Footer />
      </body>
    </html>
  );
}
